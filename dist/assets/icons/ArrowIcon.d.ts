import React from 'react';
interface ArrowIconProps {
    className?: string;
    style?: React.CSSProperties;
}
declare const ArrowIcon: {
    ({ className, style }: ArrowIconProps): JSX.Element;
    defaultProps: {
        className: string;
        style: {};
    };
};
export default ArrowIcon;
